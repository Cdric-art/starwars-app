import React from 'react';
import {useParams} from "react-router";

const FilmsId = () => {

    let { id } = useParams()

    return (
        <div>
            BLA BLA
        </div>
    );
};

export default FilmsId;
